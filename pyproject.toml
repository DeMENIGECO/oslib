[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "demenigeco-oslib"
version = "0.1.0"
description = "A Python library for OS-related utilities"
readme = "README.md"
requires-python = ">=3.9"
license = { text = "MIT" }
authors = [
    {name = "DomeniGeco"}
]

dependencies = []
